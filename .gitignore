# -----------------------------
# OS / Editor
# -----------------------------
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db

# Editor folders
.idea/
.vscode/
*.code-workspace

# -----------------------------
# Python
# -----------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Packaging / build
build/
dist/
.eggs/
*.egg-info/
*.egg

# Virtual environments
.venv/
venv/
env/
ENV/
Healthcare-insurance-dataset/.venv_aif360/

# Logs
*.log

# Coverage / testing / tooling
.coverage*
.cache/
pytestdebug.log
.tox/
.nox/
.pytest_cache/
.mypy_cache/
.ruff_cache/
coverage.xml
htmlcov/

# Jupyter
.ipynb_checkpoints/

# Environment files / secrets
.env
.env.*
*.env
*.envrc

# -----------------------------
# Project artifacts & large outputs
# -----------------------------
# Root-level outputs (legacy)
metrics/
models/
data/processed/

# Healthcare-insurance-dataset outputs
Healthcare-insurance-dataset/metrics/
Healthcare-insurance-dataset/models/
Healthcare-insurance-dataset/processed/

# Binary/model artifacts
*.joblib
*.pkl

# Plots & images generated by scripts
*.png
*.pdf
*.svg

# CSV predictions and generated reports (keep raw data by default)
**/regression_predictions.csv
**/classification_predictions.csv
**/fairness_metrics.csv
**/fairness_summary.json
**/report.md
**/data_report.md

# Node (in case of dashboard UI added later)
node_modules/

# MacOS metadata in subfolders
**/.DS_Store
AI-fairness-dashboard.zip

# Cursor IDE
.cursor/

# -----------------------------
# Allow artifacts/ directory (override global ignores)
# -----------------------------
!artifacts/
!artifacts/**
# But still exclude system junk inside artifacts
artifacts/.DS_Store
artifacts/**/.DS_Store
!.env.example

# LaTeX build intermediates
*.aux
*.bbl
*.blg
*.out
*.log
*.toc
*.fls
*.fdb_latexmk

# Built papers ARE deliverables, so they override the blanket *.pdf ignore
# above. Intermediates stay ignored by the rules just listed.
!report/*.pdf

# The paper's artifact map (scripts/artifact_map.py) references
# hmda_dataset/metrics/out_of_fold_metrics.json, and
# tests/test_artifact_map.py asserts it exists — so it must ship with the
# repo, not just live on the machine that generated it. gitignore cannot
# re-include a file whose parent directory is excluded, hence the
# three-step carve-out. IMPORTANT: commit only the ORIGINAL file from the
# machine that produced the committed fairness artifacts; regenerating it
# elsewhere yields different predictions (RF out-of-fold results vary by
# environment even at pinned versions and seed 42) and would silently
# invalidate every downstream HMDA number in the paper.
!hmda_dataset/metrics/
hmda_dataset/metrics/*
!hmda_dataset/metrics/out_of_fold_metrics.json
